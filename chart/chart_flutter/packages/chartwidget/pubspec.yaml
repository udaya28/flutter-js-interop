name: chartwidget
description: Flutter widgets for chart rendering (Canvas-based)
version: 0.1.0
publish_to: none

resolution: workspace

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: '>=3.35.0'

dependencies:
  flutter:
    sdk: flutter
  chartlib:
    path: ../chartlib

dev_dependencies:
  flutter_test:
    sdk: flutter
  lints: ^5.0.0
